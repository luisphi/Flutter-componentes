import 'package:flutter/material.dart';

class P404Page extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Ruta no encontrada'),
      ),
    );
  }
}